import CmsLoginForm from "./CmsLoginForm";

export const metadata = {
  title: "CMS Sign In | RampRate",
  robots: { index: false, follow: false },
};

export default function CmsLoginPage() {
  return <CmsLoginForm />;
}
